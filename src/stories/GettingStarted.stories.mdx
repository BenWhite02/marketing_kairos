// src/stories/GettingStarted.stories.mdx
import { Meta, Source } from '@storybook/blocks';

<Meta title="Documentation/Getting Started" />

# Getting Started with Kairos

This guide will help you get up and running with the Kairos Design System in your marketing application.

## 🚀 **Quick Start**

### Prerequisites

```bash
node >= 18.0.0
npm >= 8.0.0
react >= 18.0.0
typescript >= 4.9.0
```

### Installation

```bash
# Install the design system
npm install @kairos/design-system

# Install peer dependencies
npm install react react-dom @heroicons/react framer-motion clsx tailwind-merge

# Install development dependencies
npm install -D typescript @types/react @types/react-dom
```

### Basic Setup

1. **Import styles in your main CSS file:**

```css
/* src/index.css */
@import '@kairos/design-system/styles.css';
@import 'tailwindcss/base';
@import 'tailwindcss/components';
@import 'tailwindcss/utilities';
```

2. **Configure Tailwind CSS:**

```js
// tailwind.config.js
const kairosConfig = require('@kairos/design-system/tailwind.config');

module.exports = {
  ...kairosConfig,
  content: [
    './src/**/*.{js,jsx,ts,tsx}',
    './node_modules/@kairos/design-system/**/*.js'
  ]
};
```

3. **Set up providers:**

```tsx
// src/App.tsx
import { ThemeProvider, AccessibilityProvider } from '@kairos/design-system';

function App() {
  return (
    <ThemeProvider theme="light">
      <AccessibilityProvider>
        <YourApplication />
      </AccessibilityProvider>
    </ThemeProvider>
  );
}
```

## 🧩 **First Component**

Here's how to use your first Kairos component:

<Source code={`
import { Button, Card, CardHeader, CardBody, Input } from '@kairos/design-system';

function CreateCampaignForm() {
  const [campaignName, setCampaignName] = useState('');
  
  return (
    <Card variant="elevated" className="max-w-md">
      <CardHeader>
        <h2 className="text-xl font-semibold">Create New Campaign</h2>
      </CardHeader>
      <CardBody className="space-y-4">
        <Input
          label="Campaign Name"
          placeholder="Enter campaign name"
          value={campaignName}
          onChange={(e) => setCampaignName(e.target.value)}
          required
        />
        <Button 
          variant="primary" 
          size="lg" 
          className="w-full"
          disabled={!campaignName.trim()}
        >
          Create Campaign
        </Button>
      </CardBody>
    </Card>
  );
}
`} language="tsx" />

## 🎨 **Design System Usage**

### Colors

Use semantic color classes for consistent theming:

<Source code={`
// Primary actions
<Button variant="primary">Primary Action</Button>

// Secondary actions  
<Button variant="secondary">Secondary Action</Button>

// Semantic colors
<div className="bg-success-50 text-success-700 border border-success-200">
  Success message
</div>

<div className="bg-error-50 text-error-700 border border-error-200">
  Error message
</div>
`} language="tsx" />

### Typography

Use consistent typography classes:

<Source code={`
<h1 className="text-3xl font-bold text-gray-900">Page Title</h1>
<h2 className="text-xl font-semibold text-gray-800">Section Title</h2>
<p className="text-base text-gray-600">Body text content</p>
<span className="text-sm text-gray-500">Helper text</span>
`} language="tsx" />

### Spacing

Follow the 8px grid system:

<Source code={`
// Standard spacing scale: 4, 8, 12, 16, 24, 32, 48, 64, 96
<div className="p-4">Padding 16px</div>
<div className="m-6">Margin 24px</div>
<div className="space-y-4">Vertical spacing 16px</div>
<div className="gap-3">Gap 12px</div>
`} language="tsx" />

## 🏢 **Business Components**

Kairos includes specialized components for marketing workflows:

### Atom Card

<Source code={`
import { AtomCard } from '@kairos/design-system';

function AtomLibrary() {
  return (
    <AtomCard
      atom={{
        id: 'age-25-34',
        name: 'Age 25-34',
        type: 'demographic',
        description: 'Customers aged between 25 and 34',
        usageCount: 127,
        accuracy: 0.94
      }}
      onEdit={() => console.log('Edit atom')}
      onDelete={() => console.log('Delete atom')}
      onDuplicate={() => console.log('Duplicate atom')}
    />
  );
}
`} language="tsx" />

### Moment Card

<Source code={`
import { MomentCard } from '@kairos/design-system';

function MomentDashboard() {
  return (
    <MomentCard
      moment={{
        id: 'welcome-email',
        name: 'Welcome Email Series',
        type: 'email',
        status: 'active',
        channels: ['email', 'push'],
        deliveryRate: 0.97,
        openRate: 0.24,
        clickRate: 0.08
      }}
      onEdit={() => console.log('Edit moment')}
      onPause={() => console.log('Pause moment')}
      onDuplicate={() => console.log('Duplicate moment')}
    />
  );
}
`} language="tsx" />

## ♿ **Accessibility**

Kairos is built with accessibility in mind. All components include:

- **ARIA labels** and semantic markup
- **Keyboard navigation** support
- **Screen reader** optimization  
- **Focus management** and visual indicators
- **Color contrast** compliance (WCAG 2.1 AA)

### Accessibility Helpers

<Source code={`
import { 
  ScreenReaderOnly, 
  SkipTarget, 
  FocusRing,
  useAccessibility 
} from '@kairos/design-system';

function AccessibleComponent() {
  const { announce } = useAccessibility();
  
  return (
    <div>
      <ScreenReaderOnly>
        This content is only visible to screen readers
      </ScreenReaderOnly>
      
      <SkipTarget id="main-content" label="Main content">
        <main>
          <FocusRing color="blue">
            <button onClick={() => announce('Action completed')}>
              Accessible Button
            </button>
          </FocusRing>
        </main>
      </SkipTarget>
    </div>
  );
}
`} language="tsx" />

## 📱 **Responsive Design**

All components are mobile-first and responsive:

<Source code={`
// Responsive button sizing
<Button 
  size={{ base: 'sm', md: 'md', lg: 'lg' }}
  variant="primary"
>
  Responsive Button
</Button>

// Responsive card layout
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  <Card>Mobile: 1 col, Tablet: 2 cols, Desktop: 3 cols</Card>
</div>

// Responsive text
<h1 className="text-lg md:text-xl lg:text-2xl">
  Responsive heading
</h1>
`} language="tsx" />

## 🔧 **Advanced Features**

### Form Handling

<Source code={`
import { useForm } from 'react-hook-form';
import { Input, Button, Card } from '@kairos/design-system';

function AdvancedForm() {
  const { register, handleSubmit, formState: { errors } } = useForm();
  
  return (
    <Card>
      <form onSubmit={handleSubmit(onSubmit)}>
        <Input
          {...register('email', { required: 'Email is required' })}
          label="Email"
          type="email"
          error={errors.email?.message}
        />
        <Button type="submit" variant="primary">
          Submit
        </Button>
      </form>
    </Card>
  );
}
`} language="tsx" />

### Theme Customization

<Source code={`
import { ThemeProvider } from '@kairos/design-system';

const customTheme = {
  colors: {
    primary: {
      50: '#eff6ff',
      500: '#3b82f6',
      900: '#1e3a8a'
    }
  },
  fontFamily: {
    sans: ['Custom Font', 'Inter', 'sans-serif']
  }
};

function App() {
  return (
    <ThemeProvider theme={customTheme}>
      <YourApp />
    </ThemeProvider>
  );
}
`} language="tsx" />

## 🚀 **Next Steps**

Now that you're set up, explore:

1. **[Design System](/story/design-system-colors--page)** - Visual design tokens and guidelines
2. **[Components](/story/components-ui-button--primary)** - Browse all available components  
3. **[Business Components](/story/components-business-atomcard--default)** - Marketing-specific components
4. **[Accessibility](/story/documentation-accessibility--page)** - Accessibility features and best practices

## 🤝 **Need Help?**

- **Documentation**: Browse component stories and examples
- **GitHub Issues**: Report bugs or request features
- **Slack**: Join our developer community  
- **Email**: reach out to the design system team

---

*Happy building! 🎉*